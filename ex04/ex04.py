import random
def tableau():
    tableau_un = []
    for i in range(15):
        tableau_un.append(random.random())
    print(tableau_un)
    tableau_croissant = []
    tableau_croissant.append[tableau_un[0])
    for carac in tableau_un :
        if carac > tableau_croissant[0]
        

        
        
tableau()
